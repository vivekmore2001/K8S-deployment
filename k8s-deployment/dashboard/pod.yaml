apiVersion: v1
kind: Pod
metadata:
  name: dashboard-pod
  labels:
    name: dashboard
spec:
  containers:
  - name: dashboard
    image: vivekmore5292/dashboard:v1
    resources:
    ports:
      - containerPort: 80